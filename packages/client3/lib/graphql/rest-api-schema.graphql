type Pool {
  id: String!
  poolName: String!
  projectDetail: String!
  goalAmount: String!
  closingDate: String!
  projectCoverImage: String
  walletAddress: String!
  terms: Terms
  status: POOL_STATUS_TYPE!
  transactionHash: String
}
enum POOL_STATUS_TYPE {
  inReview
  reviewFailed
  approved
}

type Terms {
  projectGoal: String
  raisedTarget: String
}

input PendingPoolFilters {
  statusType: [POOL_STATUS_TYPE]
  hasTransactionHash: Boolean
}

extend type Query {
  pendingPools(walletAddress: String, filters: PendingPoolFilters): [Pool!]
  pool(poolId: String!): Pool
}
